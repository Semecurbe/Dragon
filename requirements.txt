# ── Drag and Rag — Python dependencies ───────────────────────────────────────
# Tested with Python 3.10+ (developed on Python 3.14.4)
#
# Install:
#   python3 -m venv env
#   source env/bin/activate          # Linux/macOS
#   pip install -r requirements.txt

# Web interface
flask>=3.1.3

# Claude API (translation + RAG answers)
anthropic>=0.104.0

# Document conversion and hierarchical chunking
docling>=2.95.0
docling-core>=2.77.0

# Vector database (local, no server needed)
chromadb>=1.5.9

# Embedding model (runs locally, no API)
sentence-transformers>=5.5.1

# PDF manipulation (chapter splitter)
PyMuPDF>=1.24.0

# PostgreSQL vector store — optional alternative to ChromaDB
# pip install psycopg2-binary pgvector
# psycopg2-binary>=2.9
# pgvector>=0.3
